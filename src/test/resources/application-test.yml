spring:
        datasource:
                driverClassName: org.h2.Driver
                url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
                username: sa
                password:
        jpa:
            hibernate:
                ddl-auto: create-drop
logging:
    file: logs/application-test.log
    level:
        root: INFO
        com.newbie.testsample: DEBUG